headers = raylib.h raymath.h rlgl.h
headerFilter = raylib.h raymath.h rlgl.h
package = raylib
staticLibraries = libraylib.a
libraryPaths = /home/lionel/work/kmp-raylib/lib/cinterop/linuxX64/raylib/lib

---

static Color lightgray  = (Color){ 200, 200, 200, 255 };  // Light Gray
static Color gray       = (Color){ 130, 130, 130, 255 };  // Gray
static Color darkgray   = (Color){  80,  80,  80, 255 };  // Dark Gray
static Color yellow     = (Color){ 253, 249,   0, 255 };  // Yellow
static Color gold       = (Color){ 255, 203,   0, 255 };  // Gold
static Color orange     = (Color){ 255, 161,   0, 255 };  // Orange
static Color pink       = (Color){ 255, 109, 194, 255 };  // Pink
static Color red        = (Color){ 230,  41,  55, 255 };  // Red
static Color maroon     = (Color){ 190,  33,  55, 255 };  // Maroon
static Color green      = (Color){   0, 228,  48, 255 };  // Green
static Color lime       = (Color){   0, 158,  47, 255 };  // Lime
static Color darkgreen  = (Color){   0, 117,  44, 255 };  // Dark Green
static Color skyblue    = (Color){ 102, 191, 255, 255 };  // Sky Blue
static Color blue       = (Color){   0, 121, 241, 255 };  // Blue
static Color darkblue   = (Color){   0,  82, 172, 255 };  // Dark Blue
static Color purple     = (Color){ 200, 122, 255, 255 };  // Purple
static Color violet     = (Color){ 135,  60, 190, 255 };  // Violet
static Color darkpurple = (Color){ 112,  31, 126, 255 };  // Dark Purple
static Color beige      = (Color){ 211, 176, 131, 255 };  // Beige
static Color brown      = (Color){ 127, 106,  79, 255 };  // Brown
static Color darkbrown  = (Color){  76,  63,  47, 255 };  // Dark Brown
static Color white      = (Color){ 255, 255, 255, 255 };  // White
static Color black      = (Color){   0,   0,   0, 255 };  // Black
static Color blank      = (Color){   0,   0,   0,   0 };  // Blank (Transparent)
static Color magenta    = (Color){ 255,   0, 255, 255 };  // Magenta
static Color raywhite   = (Color){ 245, 245, 245, 255 };  // My own White (raylib logo)